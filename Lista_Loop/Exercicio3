#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "portuguese");

    int soma = 0;
    int i;

    for(i=0;i<=100;i++)
    {
        soma = soma + i;
        printf("%i - %i\n", i, soma);
    }
    return 0;

    system("pause");
}
